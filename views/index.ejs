<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <% console.log(category_list) %>
    <% category_list.map((category) => { %>
     <a href="<%= category.url %>/items"><%= category.name %></a></p>
    <% }) %>
  </body>
</html>
